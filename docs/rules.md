# AI Rules

本文件只记录项目特有补充规则。

通用工作流、高风险操作、验证要求、最终回复格式以仓库根目录 `AGENTS.md` 为准，本文件不重复这些内容。

项目尚未正式开始开发时，`docs/next.md` 可以为空；为空表示当前没有被分配的实现任务，而不是流程错误。

## 项目补充规则

- 不做无关重构。
- 不删除已有功能，除非任务明确要求。
- 保持现有项目结构、代码风格和命名习惯。
- 开发启动后，`docs/next.md` 应始终只写一个当前可执行任务。
